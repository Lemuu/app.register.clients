<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.lemuu.cadastrodeclientes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_clients"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_clients_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CadastroDeClientes">

        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.ListClientsActivity" />
        <activity android:name=".ui.activity.FormClientActivity" />
    </application>

</manifest>